services:
  - type: web
    name: dj-acoustid-backend
    env: python

    buildCommand: |
      apt-get update
      apt-get install -y chromaprint
      which fpcalc
      pip install -r requirements.txt

    startCommand: gunicorn app:app
