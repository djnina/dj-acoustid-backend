services:
  - type: web
    name: dj-acoustid-backend
    env: python

    buildCommand: |
      apt-get update
      apt-get install -y ffmpeg chromaprint
      which fpcalc
      fpcalc --version
      pip install -r requirements.txt

    startCommand: python app.py
